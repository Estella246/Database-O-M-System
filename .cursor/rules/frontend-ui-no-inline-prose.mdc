---
description: 前端页面避免表单区长备注与多余「重新加载」按钮（参数/工单 UI 克制）
globs: frontend/**/*
alwaysApply: false
---

# 前端 UI：少说明、少手动刷新

## 不要在主表单区堆「备注」

- 在**标题与表单/选项卡之间**，不要新增整段的**操作说明、使用须知、灰底引言式备注**（例如 `params-page-intro` 风格的长文案）。
- 产品说明、占位符含义、与弹窗的差异等，写在 **`docs/`** 或需求文档即可，不要塞进页面当装饰。
- **保留**短小的错误/成功条（banner）、字段 `label`、必填提示、无障碍 `aria-label` 等**必要** UI 文案。

## 不要为单页数据加「重新加载」

- **不要**在参数配置子页、工单相关表单页顶栏放置 **「重新加载」** 按钮（全站一致，含版本模块等历史页）。
- 数据依赖：**进入该页签/路由**时的自动拉取、**保存成功后**的刷新、`groupTemplateNeedsRefresh` / `versionNeedsRefresh` 等与现有模式一致即可。

## 非要求的前端显示字段不可擅自增加

- **非要求的前端显示字段，不可以自己加。** 凡页面上新增的文案、标签、统计行、头像/账号展示等，须以产品/需求或明确指派为准；不要出于「更完整」自行补充展示项。

## 实现时注意

- 改拉群模版、同类参数页时，对齐本规则；不要在 `render*PageHtml` 里再拼大段说明性 `<p class="params-page-intro">…</p>`。
