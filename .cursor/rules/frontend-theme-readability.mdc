---
description: 前端样式改动需保证可读性，尤其暗黑主题下提升文字对比度
globs: frontend/**/*.css,frontend/**/*.js
alwaysApply: false
---

# 前端主题可读性约束

## 核心要求

- 在进行前端样式修改时，必须同时检查文字可读性，避免出现「字发灰、对比不足、层级不清」问题。
- 涉及暗黑主题（`html[data-theme="dark"]`）的新增或改动样式时，优先保证信息可读，再追求视觉风格。

## 暗黑主题适配要求

- 标题、分组标题、标签类文字应有清晰层级：主标题 > 分组标题 > 普通标签文字。
- 关键文字（标题、当前激活项、重要标签）在暗色背景上应提升亮度，必要时增加轻量 `text-shadow` 辅助识别。
- 标签/按钮背景与边框需和文字形成足够对比；`default / hover / active` 三态都要可辨认。
- 不要只改背景不改文字色；也不要只改文字色导致 hover/active 状态不明显。

## 交互与收起态

- 新增样式需兼容侧边栏收起态（`left-collapsed`），不能破坏既有的收起视觉与交互。
- 展开态新增的分组容器、标题、标签态样式，在收起态需要有对应重置或降级样式。

## 提交前自检

- 至少在浅色主题和暗黑主题下各检查一次：标题是否清晰、标签是否可读、active 是否突出。
- 如果是导航/侧栏改动，需额外检查收起态表现是否与既有行为一致。
