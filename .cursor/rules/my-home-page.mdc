---
description: 「我的主页」为站点默认入口；工单列表工作台路径与 activeKey 约定
globs: frontend/**/*.js
alwaysApply: false
---

# 我的主页（默认落地页）

## 路由与状态

- 浏览器打开站点根路径 **`/`** 时，应对 **`activeKey === "home"`** 的「我的主页」视图（侧边栏首项文案 **我的主页**）。
- 工单列表「工作台」不在根路径：URL 为 **`/workbench`**（及可选尾部 `/`），对应 **`activeKey === "list"`**。
- 前端 `getUrlByKey`：`home` → `/`，`list` → `/workbench`。`syncActiveKeyFromPath` 须与上述一致；无法识别的 path 宜回落到 **我的主页**（`ensureHomeTab()`），避免误进工作台。

## 页签与侧边栏

- `state.openTabs` 中 **我的主页** 页签 `key` 为 **`home`**，`label` 为 **我的主页**，**不可关闭**（`closable: false`），与原先工作台独占非关闭页签的角色对齐在「默认入口页」上。
- **工作台** 页签在首次进入列表时通过 **`ensureListTab()`** 加入，可为可关闭（`closable: true`），以便与多页签并存。
- 侧栏点击 **我的主页** / **工作台** 时须调用 **`ensureHomeTab()`** / **`ensureListTab()`** 再切换 `activeKey`，与值班表、参数页等模式一致。

## 走单日历（热力图）

- 展示在 **我的主页**（`activeKey === "home"`）内，统计最近约 **365 天**（按本地日历日、周日起算的列布局）。
- **走单量**口径：与列表「我创建」一致 — **创建人为当前登录操作人**（`ticketCreatorMatchesOperator`）的工单，按 **`createdAt`（优先）/ `startDate`（回落）** 对应的**本地日期**计入当天条数。
- **总走单**：上述时间窗口内走单量之和。
- **颜色深度**：相对本人窗口内**最大单日走单量**分档着色；无单为浅灰底。
- **悬停**：使用 **`position: fixed`** 浮层（实现上可挂到 `document.body` 避免被主区 `overflow` 裁剪），优先出现在格子**上方**，空间不足时改到下方；**勿**在页面底部静态展示提示文案。
- **月份与周列对齐**：月标签与热力图共用 **同宽周列（12px）** 与 **统一 `gap`（如 2px）**；月文案可用 **8px** 字号并 **`overflow: visible`**，避免「月」被裁切。
- **外框样式**：走单热力图容器与值班表月历块一致，复用 **`duty-roster-card` + `duty-roster-card--calendar`**（白半透明底、14px 圆角、`0 8px 32px` 浅阴影、白边）。
- **格子间距**：热力图使用 **统一 `gap`（如 2px）** 作为列距与行距；周列宽与格子边长一致（如 **12px**）；左侧星期列 **row-gap** 与格子一致。
- **概要区**：**勿**在「概要」下擅自增加姓名单独一行等未在需求中列出的展示字段；主页内**不提供**「进入工作台」按钮（仍可通过侧栏 / 页签进工作台）。
- **总走单**与 **最活跃月/天** 文案使用与图例 **「更少」** 一致的 **11px** 级正文字号。
- **底部统计**：**最活跃月** 与 **最活跃天** 同一行、相邻展示（ flex，`justify-content: center`），勿分两列左右对齐。

## 与其它规则的关系

- 工单列表列、排序、页签过滤等仍见 **`.cursor/rules/home-ticket-list-columns.mdc`**（实现于 **`activeKey === "list"`** 的工作台，而非根路径 `/`）。
