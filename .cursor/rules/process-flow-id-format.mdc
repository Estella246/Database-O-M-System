---
description: 新建工单时流程 ID / ticket_no 的统一编号格式（YW + 日期 + 三位序号）
globs: frontend/**/*.js, backend/**/*.py
alwaysApply: false
---

# 流程 ID / 工单号（`ticket_no`）

凡**新建工单**（列表「创建」、弹窗起单、将来任意 API/脚本建单），对外展示的 **流程 ID** 与库表 `ticket.ticket_no`（及与之一致的 `process_flow_id` 等）须统一为：

## 格式

`YW` + `YYYYMMDD` + `nnn`

- **`YW`**：固定前缀（大写）。
- **`YYYYMMDD`**：建单**自然日**（按服务端日历；与前端本机日期在绝大多数场景一致）。
- **`nnn`**：全局序号，**十进制三位**，范围 **`000`–`999`**，不足三位左补零；**跨自然日连续递增**，用尽后从 `000` 循环（**不按日重置**）。  
  1000 个号循环用尽且当日前缀下均已被占用时须报错或产品级扩容（勿静默截断为四位）。

示例：`YW20260402001`（2026-04-02 建单，三位为 `001`）；若前一日最后号为 `042`，则次日首号为 `YW20260403043`。

## 分配规则

- **工作台/补丁「创建」弹窗**：打开时使用本地 `draft-{uuid}` 草稿 ID（`makeCreateDraftTicketId()`），**不**调用 `allocate-no`、**不**推进 `ticket_global_seq`；**首次 submit** 时由 `_get_or_create_ticket` 分配正式 `YW…` / `HPM…`，响应 `ticket_id` 与落库一致，前端 `remapTicketOrderId` 重映射草稿状态。
- **全局序号**：服务端 `ticket_global_seq`（迁移 0089）维护 `a`（000–999）；`allocate_yw_ticket_no` / `allocate_hpm_ticket_no` 在**落库或脚本预取**时 +1（mod 1000）；若与当日前缀组合后已存在则继续递增直至未占用。并发用事务内咨询锁。
- **非法或非规范单号**（含 `draft-*`、历史 `N`+时间戳）：首次落库建单时由后端改写为规范格式，并在提交响应中返回实际 `ticket_id`；前端须用返回单号重映射会话内状态。

## 实现位置

- 前端草稿 ID：`makeCreateDraftTicketId()` / `isCreateDraftTicketId()`；创建弹窗 `beginCreateTicketModal` / `beginPatchCreateTicketModal`。
- 脚本/API 预取：`POST /api/tickets/allocate-no`（非创建弹窗路径）。
- 后端：`_get_or_create_ticket` 与 `POST .../submit` 响应中的 `ticket_id`（等于落库 `ticket_no`）。

## 关联

- 首页列表「流程 ID」列说明见 `.cursor/rules/home-ticket-list-columns.mdc`。
