---
description: 新建工单时流程 ID / ticket_no 的统一编号格式（YW + 日期 + 三位序号）
globs: frontend/**/*.js, backend/**/*.py
alwaysApply: false
---

# 流程 ID / 工单号（`ticket_no`）

凡**新建工单**（列表「创建」、弹窗起单、将来任意 API/脚本建单），对外展示的 **流程 ID** 与库表 `ticket.ticket_no`（及与之一致的 `process_flow_id` 等）须统一为：

## 格式

`YW` + `YYYYMMDD` + `nnn`

- **`YW`**：固定前缀（大写）。
- **`YYYYMMDD`**：建单**自然日**（按服务端日历；与前端本机日期在绝大多数场景一致）。
- **`nnn`**：当日序号，**十进制三位**，范围 **`000`–`999`**，不足三位左补零。  
  当日用尽 1000 个号后须报错或产品级扩容（勿静默截断为四位）。

示例：`YW20260402001`（2026-04-02 当日第 1 号，三位为 `001`）。

## 分配规则

- **当日序号**：在**同一天**内，**优先使用当日尚未占用的最小编号**（便于与「循环使用 000–999」一致，并在工单删除/归档后重用小号）。  
  前端本地可用 `localStorage` 做递增提示；**以服务端落库为准**（并发时用事务内咨询锁 + 查询已占用后缀，避免重号）。
- **非法或非规范 URL 单号**（如历史 `N`+时间戳）：首次落库建单时由后端**改写**为上述 `YW…` 格式，并在提交响应中返回实际 `ticket_id`；前端须用返回单号**重映射**会话内状态（Tab、表单缓存、`workflowByOrderId` 等）。

## 实现位置

- 前端：`makeNewTicketId()`（及所有调用它的建单入口）。
- 后端：`_get_or_create_ticket` 内对 `ticket_no` 的校验、分配与 `POST .../submit` 响应中的 `ticket_id`（等于落库 `ticket_no`）。

## 关联

- 首页列表「流程 ID」列说明见 `.cursor/rules/home-ticket-list-columns.mdc`。
