---
description: 新增或执行 SQL 统一追加到 db/migrations；不再同步 db/postgres
globs: db/**/*.sql
alwaysApply: false
---

# SQL 迁移追加规则

## 何时做（强制）

在以下任一情况发生后，**必须**在 **`db/migrations/`** 下新增/追加迁移 SQL（append-only，不改写已执行历史）：

- 新增数据库变更（DDL/DML）；
- 在会话中执行了需要落库留痕的 SQL。

## 执行原则

- 新 SQL 直接写入 `db/migrations/`（新编号文件或当前未执行迁移文件末尾追加）。
- 不要求、也不需要同步修改 `db/postgres/postgres_sync_changelog.sql`。
- 变更 SQL 需要**直接执行**，不要只停留在前端或文档层。

## 文件命名与追加

- 迁移文件名遵循现有编号序列：`NNNN_description.sql`。
- 采用 append-only：已有迁移文件若已执行，不做覆盖式重写。
- SQL 语法以 PostgreSQL 可执行为准（若存在方言差异需在迁移内注释说明）。

## 自检

- [ ] SQL 已追加到 `db/migrations/`（非 `db/postgres`）
- [ ] 迁移编号与命名符合现有规则
- [ ] SQL 已在数据库执行并可通过
