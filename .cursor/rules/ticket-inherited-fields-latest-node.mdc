---
description: 多节点继承同名字段在展示、统计、导出时以流程中最靠后节点为准
globs:
  - frontend/**/*.js
  - backend/**/*.py
alwaysApply: false
---

# 继承字段与多节点同键：统一取值（最后节点优先）

凡在流程**多个节点**中因继承、白名单或表单配置而重复出现的**同一业务字段**（同一 `field_key` 或产品语义上的同一字段，例如「问题引入模块」在「运维分析」「开发分析」均有）：

## 原则

**展示**（首页列表列、详情页各节点只读区/展开卡片、步骤条相关文案等）、**统计**、**导出**（Excel/CSV、批量接口等）在需要从多条 `ticket_node_data`（或等价落库结构）**合并为单值或单列**时，对该字段必须 **以流程顺序上「最后出现」的节点中的内容为准**：

- 在**有向流程**中，**更靠后的节点**覆盖**更靠前的节点**；以后序节点已落库的**非空**取值优先。
- **勿**仅以全局 `created_at` 决定覆盖关系并据此与产品流程先后冲突；若某场景下时间与流程序一致，实现上仍应**以流程节点顺序为语义来源**，时间仅用于「同一节点内多条提交」的先后。

## 示例

「问题引入模块」在 **运维分析** 与 **开发分析** 均有；流程上 **开发分析在运维分析之后** → 合并展示、统计、导出时，取 **开发分析** 节点下该字段的已提交内容。若开发分析侧尚无该键或非空值，再回退到运维分析（及更前序）中该字段的合并结果。

## 实现要点

1. **节点先后**：与 `workflow_node`、流转边或产品约定的 `node_key` 顺序一致（拓扑上靠近闭环/下游者为「后」）。
2. **同一节点内**若有多条 `ticket_node_data`：对该字段键取该节点内**按提交时间最后一次非空**。
3. **列表、详情、统计任务、导出**应复用**同一套**归并规则，避免口径分裂。
4. 与仅针对「某一列」的列说明（如 `.cursor/rules/home-ticket-list-columns.mdc`）冲突时，以本条**全局覆盖语义**为准，并同步收敛列规则表述。

## 关联

- 首页列表各列数据来源：`.cursor/rules/home-ticket-list-columns.mdc`
- 详情页已走过节点展示：`.cursor/rules/ticket-detail-passed-nodes.mdc`
