---
description: 问题详情页 — 已走过节点须展示卡片且可展开查看字段详情
globs: frontend/**/*.js
alwaysApply: false
---

# 问题详情页：已走过节点的展示与展开

**对已流转经过的节点（非当前仅占位）：**

1. **节点卡片**：在步骤条下方仍要展示对应节点的卡片（与流程一致，不能只留空）。
2. **展开后要有字段详情**：用户点击/展开该节点卡片时，必须展示该节点当时已填报的**具体字段与取值**（只读展示即可），不能仅用「暂无处理内容」等占位文案糊弄过去。
3. **数据来源**：字段详情应来自后端该工单、该 `node_key` 下已持久化的数据（如 `GET .../nodes/{node_key}/data` 或等价接口）；无数据时再显示合理的空态说明，并区分「尚无提交」与「接口失败」。

## 与现有约定的关系

- 与 `.cursor/skills/workflow-node-visibility/SKILL.md` 一致：非当前节点不展示 `处理方式*`、`下一步处理人*` 等流转控件；但**已走过节点**在展开区域必须能看到**业务字段**的历史内容。
- 当前节点：保持可编辑与自动展开规则不变。

## 实现时注意

- 占位文案「暂无处理内容」仅适用于确实没有可展示字段定义或没有落库数据的场景；若库里有数据，必须在展开后渲染出来。
