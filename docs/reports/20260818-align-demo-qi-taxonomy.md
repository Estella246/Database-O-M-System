# 质量改进领域分析全空修复（演示数据对齐责任田树）：测试与修改报告

- **日期**：2026-08-18
- **分支**：`qi_analytic_improve`（基于 `c04c541`）
- **任务**：改进报告「三、质量改进领域分析」当前都是空的
- **流程**：ops-dev-workflow 5 阶段闭环（任务 #56-#60）

## 一、任务概述与根因

**现象**：改进报告第三段导入后仅输出 3 个二级模块（SQL引擎/CBB、SQL引擎/驱动、管控问题/管控子模块），
每个模块 `total=0`、全部图表空。

**根因（非代码缺陷）**：该段按设计**以责任田树的二级模块为骨架**聚合（m21 契约锁定：输出树全量二级模块、
无数据计 0），而本地演示数据与树完全无交集——

1. 本地树 = m02 工单测试夹具树（`test/test_data.json`：SQL引擎>CBB/驱动(JDBC,ODBC) +
   管控问题>管控子模块>管控叶子；m02 相关用例 `finally` 整体还原成它，是本地树的稳定态）；
2. 517 条 DENSE 演示数据使用合成标签 `领域01..52 / 模块01..60`（488 个组合）；另 18 条 SQL引擎
   行的模块（测试模块/备份恢复/JDBC接口/ODBC/主备切换）也不在树的二级模块下 → 聚合全为 0。

远程 47.95.244.175 尚为旧代码（无改进报告接口），本现象仅本地；但远程数据域（性能优化/数据库内核…）
与远程树（0021 种子仅 SQL引擎）同样不对齐，**新代码部署远程后该段也会全空**，需另行对齐（见遗留事项）。

## 二、计划与评审记录

- AskUserQuestion 两轮：①修复方向——「A 数据对齐到夹具树」（否决 B 重建真实风格树、C 代码侧数据兜底）；
  ②方案评审——「通过，开始开发」。无偏离（映射表按数据实况把 模块01..08 修正为 模块01..60 按模 8 轮转，
  属实现细节，方向与口径不变）。

## 三、修改清单

| 项 | 变更 |
|---|---|
| `scripts/align_demo_qi_duty_taxonomy.py`（新增） | 确定性重映射脚本：`领域NN→NN 偶数?SQL引擎:管控问题`；`模块NN→该域 8 条深层路径按 (NN-1) mod 8 轮转`（SQL引擎：CBB、CBB/语法兼容、CBB/执行器、驱动、驱动/JDBC、驱动/ODBC、驱动/JDBC/连接池、驱动/ODBC/游标；管控问题：管控子模块、管控子模块/管控叶子、配额管理、权限管控、审计策略、租户隔离、管控叶子/限流、备份策略）；存量 18 条杂项就近归位（JDBC接口→驱动/JDBC、ODBC→驱动/ODBC、备份恢复→CBB/备份恢复、主备切换→CBB/主备切换、测试模块→CBB/测试模块）。单事务 UPDATE；跑前导出 `id/domain/module_feature` 到 /tmp TSV；`--dry-run` 只统计；`--restore TSV` 回滚；幂等（无目标行即 no-op）；内置 SQL 后验（窗口计数 + 剩余合成行=0） |
| 本地库 `qi_request`（数据） | 535 行 `domain/module_feature` 重映射（517 合成 + 18 杂项）；阶段/类型/人/日期/状态等列不动 |

**无产品代码改动**（后端/前端/迁移零变更）。

## 四、接口测试结果（阶段 3）

- `test_m21_improvement_report.py`（改进报告契约，自有种子）：**17 passed**。
- `test_m20_qi_workflow.py`（qi 全流程，自有种子）：**126 passed**。
- 直接验证（live :8000）：
  - `GET /api/improvement-report/202608/import/domain?operator_id=admin` → 3 模块
    `SQL引擎/CBB total=103（stage 5 档/类型 7 档/提交人 52/待处理 3/rf=备份恢复田 接纳99%）`、
    `SQL引擎/驱动 total=171（…rf=ODBC田）`、`管控问题/管控子模块 total=255`；
  - 同接口 `202607` → 84/143/227（YTD 窗口含 7 月数据）；
  - 脚本 `--dry-run` 复跑 no-op（幂等）；`--restore` 从备份恢复后 领域XX=517、杂项=18（精确回到初始态），
    再执行回到对齐态 103/171/255（回滚路径可用）。

## 五、系统测试结果（阶段 4）

- `test/e2e/test_e2e_improvement_report.py`：**10 passed**（报告页四段导入/保存/归档端到端回归）。
- `test/e2e/test_qi_page_no_errors.py`（单独跑）：**4 passed**（qi 页含分析 tab 无 JS 错误）。
- 无前端代码改动，jest 不涉及（改动清单无前端项）。
- 关键证据：第三段三模块图表全部有数（阶段饼 5 档、类型饼 7 档、提交人 50+），其中两个模块带上
  既有在研田绑定（备份恢复田/ODBC田）的三率——重映射使演示绑定开始命中数据。

## 六、测试覆盖核对（改动清单逐项）

| 改动点 | 覆盖 |
|---|---|
| 脚本：主执行路径（备份+重映射+后验） | 实跑两次（首轮 84 行暴露映射表按 01..08 设计错误→修正为 mod 8；终轮 535 行），API+SQL 双面断言 |
| 脚本：`--dry-run` 分支 | 实跑（对齐后 no-op 输出 0/0） |
| 脚本：`--restore` 回滚分支 | 实跑（517/18 精确还原，再对齐收尾） |
| 脚本：领域奇偶/模块取模/杂项归位映射函数 | 全部 535 行经主路径验证（后验无「其它」桶残留） |
| 数据变更影响面（报告第三段/分析接口） | m21 17 + m20 126 回归 + import/domain 双月断言 |
| 页面级渲染 | e2e 改进报告 10 + qi 页无错 4 |

**覆盖缺口：无。**

## 七、遗留风险与后续事项

- 分析 tab「领域分布/领域占比」由 53 个合成域变为 2 个树域（模块&特性饼仍有 ~16 个深层路径）；
  如需更丰富的领域演示，需按方向 B 重建树+数据（已被否决，可再议）。
- 已保存的 202608（及更早）报告**草稿不会自动刷新**，需在页面重新点「导入」核对后保存。
- m02 树用例仍会把树还原为夹具树——本方案数据即对齐夹具树，树还原后第三段依旧有数（设计如此）。
- **远程部署提醒**：新代码上远程后，远程 qi 数据域与远程树同样不对齐，第三段会全空；届时可移植本脚本
  的映射思路按远程树taxonomy 对齐远程数据（或反向扩充远程树），需单独任务处理。
- 本地库 DENSE 演示数据口径已变化（领域XX/模块XX 不复存在），记忆条目已同步更新。
